fn main() {
    let t = (1, 2, "3");
    println!("{} {} {}", t.0, t.1, t.2);
    println!("{:?}", t);

    // タプルはコピーされる (保持する値がコピー可能な場合)
    let t1 = (1, '2');
    let t2 = t1;
    println!("{:?} {:?}", t1, t2);
}
